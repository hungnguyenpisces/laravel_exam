version: '3.3'

services:
  mysqldb:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - '3306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: laravel_exam
      MYSQL_DATABASE: laravel_exam